server:
  port: 9101
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #Nacos地址 作为注册中心地址
      config:
        server-addr: localhost:8848 #Nacos地址 作为配置中心地址
        file-extension: yaml #这里我们获取的yaml格式的配置